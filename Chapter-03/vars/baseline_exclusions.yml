# vars/baseline_exclusions.yml

baseline_exclusions:
  #- '01.01'
  # Ensure sudo is installed
  #- '01.02'
  # Ensure sudo log file exists
  #- 02.01
  # Ensure message of the day is configured properly
  #- 02.02
  # Ensure local login warning banner is configured properly
  - '100.100'